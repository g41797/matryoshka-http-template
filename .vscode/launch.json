{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Tests",
            "program": "${workspaceFolder}/debug_current",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Run Tests",
            "terminal": "integrated"
        }
    ]
}
